services:
  app:
    build: .
    expose:
      - "3859:3859"
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - default

networks:
  default:
    external: true
    name: easypanel
